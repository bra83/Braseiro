{
  "name": "braseiro-ose-web-ui",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && mkdir -p dist/assets && cp src/index.html src/styles.css src/strict-overrides.css dist/ && cp -R src/assets/. dist/assets/ && cp ../assets/ASSET_MANIFEST.json dist/ASSET_MANIFEST.json",
    "test:capture": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "typescript": "5.6.3"
  }
}
